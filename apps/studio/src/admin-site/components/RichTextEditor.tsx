import CharacterCount from "@tiptap/extension-character-count";
import Image from "@tiptap/extension-image";
import Link from "@tiptap/extension-link";
import Placeholder from "@tiptap/extension-placeholder";
import StarterKit from "@tiptap/starter-kit";
import { EditorContent, useEditor } from "@tiptap/react";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Braces, Code2, ImageIcon, ImagePlus, Link2, List, ListOrdered, Loader2, Quote, Redo2, RemoveFormatting, Search, Sparkles, Undo2, WandSparkles } from "lucide-react";
import { useEffect, useRef, useState } from "react";
import { Streamdown } from "streamdown";
import { toast } from "sonner";
import { trpc } from "@/lib/trpc";

type EditorValue = { type: string; content?: unknown[]; [key: string]: unknown };
type LibraryAsset = { id: string; url: string; filename: string; alt_text: string | null; mime_type: string; folder: string };

export function RichTextEditor({ postId, title, initialContent, onChange }: { postId?: string; title: string; initialContent?: EditorValue | null; onChange: (value: { json: EditorValue; html: string }) => void }) {
  const fileRef = useRef<HTMLInputElement>(null);
  const [aiAction, setAiAction] = useState<"outline" | "improve" | "meta" | "summarize">("improve");
  const [aiResult, setAiResult] = useState("");
  const [mediaOpen, setMediaOpen] = useState(false);
  const [mediaSearch, setMediaSearch] = useState("");
  const [sourceMode, setSourceMode] = useState<"visual" | "json" | "html">("visual");
  const [sourceValue, setSourceValue] = useState("");
  const media = trpc.studio.media.list.useQuery({ search: mediaSearch || undefined }, { enabled: mediaOpen });
  const upload = trpc.studio.media.upload.useMutation({ onSuccess: asset => { insertAsset(asset); toast.success("Image added to the story."); }, onError: error => toast.error(error.message) });
  const ai = trpc.ai.assist.useMutation({ onSuccess: result => setAiResult(result.content), onError: error => toast.error(error.message) });
  const editor = useEditor({ extensions: [StarterKit.configure({ heading: { levels: [1, 2, 3] } }), Image.configure({ allowBase64: false }), Link.configure({ openOnClick: false, HTMLAttributes: { rel: "noopener noreferrer", target: "_blank" } }), Placeholder.configure({ placeholder: "Start with the idea. The shape will follow." }), CharacterCount], content: (initialContent as any) || { type: "doc", content: [] }, editorProps: { attributes: { class: "editor-content px-6 py-7" } }, onUpdate: ({ editor }) => onChange({ json: editor.getJSON() as EditorValue, html: editor.getHTML() }) });
  useEffect(() => { if (editor && initialContent) editor.commands.setContent(initialContent as any, { emitUpdate: false }); }, [editor, initialContent]);
  useEffect(() => { if (!editor) return; setSourceValue(sourceMode === "json" ? JSON.stringify(editor.getJSON(), null, 2) : editor.getHTML()); }, [editor, sourceMode]);
  const insertAsset = (asset: LibraryAsset) => { if (!asset.mime_type.startsWith("image/")) { toast.info("Only image assets can be embedded in rich text. Link to documents from a paragraph instead."); return; } editor?.chain().focus().setImage({ src: asset.url, alt: asset.alt_text || asset.filename }).run(); setMediaOpen(false); };
  const addLink = () => { const href = window.prompt("Paste a secure https:// link"); if (!href) return; if (!/^https?:\/\//i.test(href)) { toast.error("Please use an http or https URL."); return; } editor?.chain().focus().extendMarkRange("link").setLink({ href }).run(); };
  const uploadFile = async (file?: File) => { if (!file) return; const allowed = ["image/jpeg", "image/png", "image/webp", "image/gif", "image/svg+xml"]; if (!allowed.includes(file.type)) { toast.error("Choose a JPEG, PNG, WebP, GIF, or SVG image."); return; } if (file.size > 10 * 1024 * 1024) { toast.error("Images must be 10 MB or smaller."); return; } const base64 = await new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(String(reader.result)); reader.onerror = () => reject(new Error("Could not read the selected file.")); reader.readAsDataURL(file); }); upload.mutate({ filename: file.name, mimeType: file.type as "image/jpeg" | "image/png" | "image/webp" | "image/gif" | "image/svg+xml", base64, folder: "editor-images", altText: "" }); };
  const runAi = () => { if (!postId) { toast.info("Save this post first to unlock its AI writing assistant."); return; } const content = editor?.getText().trim(); if (!content) { toast.info("Add a little draft text first, then ask the assistant for help."); return; } ai.mutate({ postId, action: aiAction, title: title || "Untitled post", content }); };
  const applySource = () => { if (!editor) return; try { if (sourceMode === "json") { const parsed = JSON.parse(sourceValue); if (!parsed || typeof parsed !== "object" || !("type" in parsed)) throw new Error("A structured document must include a type."); editor.commands.setContent(parsed); } else { if (/<\s*(script|iframe|object|embed|style)\b/i.test(sourceValue) || /on\w+\s*=/i.test(sourceValue)) throw new Error("Scripts, embeds, event handlers, and style tags are not allowed in controlled HTML mode."); editor.commands.setContent(sourceValue); } toast.success("Source applied to the structured document. Saving will sanitize the resulting HTML on the server."); setSourceMode("visual"); } catch (error) { toast.error(error instanceof Error ? error.message : "The source could not be applied."); } };
  const toolbar = (label: string, active: boolean, onClick: () => void, icon: React.ReactNode) => <Button type="button" variant={active ? "secondary" : "ghost"} size="icon" className="h-8 w-8" title={label} aria-label={label} onClick={onClick}>{icon}</Button>;
  return <div className="overflow-hidden rounded-xl border border-border bg-white shadow-sm"><div className="flex flex-wrap items-center gap-0.5 border-b border-border bg-[#fbfcfa] px-3 py-2">{toolbar("Undo", false, () => editor?.chain().focus().undo().run(), <Undo2 className="h-4 w-4" />)}{toolbar("Redo", false, () => editor?.chain().focus().redo().run(), <Redo2 className="h-4 w-4" />)}<span className="mx-1 h-5 w-px bg-border" />{toolbar("Heading", editor?.isActive("heading") || false, () => editor?.chain().focus().toggleHeading({ level: 2 }).run(), <span className="text-xs font-bold">H2</span>)}{toolbar("Bold", editor?.isActive("bold") || false, () => editor?.chain().focus().toggleBold().run(), <span className="font-serif text-sm font-bold">B</span>)}{toolbar("Italic", editor?.isActive("italic") || false, () => editor?.chain().focus().toggleItalic().run(), <span className="font-serif text-sm italic">I</span>)}{toolbar("Quote", editor?.isActive("blockquote") || false, () => editor?.chain().focus().toggleBlockquote().run(), <Quote className="h-4 w-4" />)}{toolbar("Bullet list", editor?.isActive("bulletList") || false, () => editor?.chain().focus().toggleBulletList().run(), <List className="h-4 w-4" />)}{toolbar("Numbered list", editor?.isActive("orderedList") || false, () => editor?.chain().focus().toggleOrderedList().run(), <ListOrdered className="h-4 w-4" />)}<span className="mx-1 h-5 w-px bg-border" />{toolbar("Add link", editor?.isActive("link") || false, addLink, <Link2 className="h-4 w-4" />)}{toolbar("Choose from media library", false, () => setMediaOpen(true), <ImageIcon className="h-4 w-4" />)}{toolbar("Upload image", false, () => fileRef.current?.click(), <ImagePlus className="h-4 w-4" />)}{toolbar("JSON source", sourceMode === "json", () => setSourceMode(sourceMode === "json" ? "visual" : "json"), <Braces className="h-4 w-4" />)}{toolbar("Controlled HTML", sourceMode === "html", () => setSourceMode(sourceMode === "html" ? "visual" : "html"), <Code2 className="h-4 w-4" />)}{toolbar("Clear formatting", false, () => editor?.chain().focus().unsetAllMarks().clearNodes().run(), <RemoveFormatting className="h-4 w-4" />)}<input ref={fileRef} type="file" accept="image/jpeg,image/png,image/webp,image/gif,image/svg+xml" className="hidden" onChange={event => { uploadFile(event.target.files?.[0]); event.currentTarget.value = ""; }} /></div>{sourceMode === "visual" ? <EditorContent editor={editor} /> : <div className="p-5"><p className="mb-3 text-xs text-muted-foreground">{sourceMode === "json" ? "Structured source is the canonical editor representation." : "Controlled HTML is converted back to structured content and sanitized again during server save."}</p><textarea value={sourceValue} onChange={event => setSourceValue(event.target.value)} className="min-h-[360px] w-full rounded-lg border border-border bg-[#101b15] p-4 font-mono text-xs leading-5 text-[#e5f1e5]" spellCheck={false} /><div className="mt-3 flex justify-end gap-2"><Button type="button" variant="outline" onClick={() => setSourceMode("visual")}>Cancel</Button><Button type="button" onClick={applySource}>Apply to document</Button></div></div>}<div className="flex items-center justify-between border-t border-border bg-[#fbfcfa] px-5 py-2 text-xs text-muted-foreground"><span>{editor?.storage.characterCount.words() ?? 0} words</span><span>{editor?.storage.characterCount.characters() ?? 0} characters</span></div><div className="border-t border-[#cad8c5] bg-[#eef4ea] p-4"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-2"><span className="grid h-7 w-7 place-items-center rounded-md bg-primary text-primary-foreground"><Sparkles className="h-4 w-4" /></span><div><p className="text-sm font-semibold">Writing companion</p><p className="text-xs text-muted-foreground">Suggestions stay in the editor until you choose to use them.</p></div></div><div className="flex gap-2"><select value={aiAction} onChange={event => setAiAction(event.target.value as typeof aiAction)} className="h-9 rounded-md border border-border bg-white px-2 text-sm"><option value="outline">Build outline</option><option value="improve">Suggest improvements</option><option value="meta">Write SEO metadata</option><option value="summarize">Summarize draft</option></select><Button type="button" size="sm" onClick={runAi} disabled={ai.isPending} className="gap-2">{ai.isPending ? <Loader2 className="h-4 w-4 animate-spin" /> : <WandSparkles className="h-4 w-4" />}Ask AI</Button></div></div>{aiResult && <div className="mt-4 rounded-lg border border-[#cbd8c5] bg-white p-4 text-sm"><div className="mb-2 flex items-center justify-between"><p className="font-label text-[10px] text-primary">AI suggestion</p><Button type="button" variant="ghost" size="sm" onClick={() => navigator.clipboard.writeText(aiResult).then(() => toast.success("Suggestion copied"))}>Copy</Button></div><Streamdown>{aiResult}</Streamdown></div>}</div><Dialog open={mediaOpen} onOpenChange={setMediaOpen}><DialogContent className="max-w-3xl"><DialogHeader><DialogTitle className="font-display text-2xl">Choose from the media library</DialogTitle><DialogDescription>Reusable images are stored securely and delivered from the platform media library.</DialogDescription></DialogHeader><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" /><Input value={mediaSearch} onChange={event => setMediaSearch(event.target.value)} placeholder="Search filenames" className="pl-10" /></div><div className="grid max-h-[50vh] grid-cols-2 gap-3 overflow-y-auto sm:grid-cols-3">{media.isLoading ? <div className="col-span-full grid min-h-32 place-items-center"><Loader2 className="h-5 w-5 animate-spin text-primary" /></div> : media.data?.filter(asset => asset.mime_type.startsWith("image/")).map(asset => <button type="button" key={asset.id} onClick={() => insertAsset(asset)} className="overflow-hidden rounded-lg border border-border text-left transition hover:border-primary hover:ring-2 hover:ring-primary/15"><img src={asset.url} alt={asset.alt_text || ""} className="aspect-[4/3] w-full object-cover" /><span className="block truncate px-2 py-2 text-xs font-medium">{asset.filename}</span><span className="block truncate px-2 pb-2 text-[10px] text-muted-foreground">{asset.folder}</span></button>)}{!media.isLoading && !media.data?.some(asset => asset.mime_type.startsWith("image/")) && <p className="col-span-full py-10 text-center text-sm text-muted-foreground">No reusable images match this search.</p>}</div></DialogContent></Dialog>{upload.isPending && <div className="absolute"><Loader2 className="animate-spin" /></div>}</div>;
}
