import DashboardLayout from "@/admin-site/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { trpc } from "@/lib/trpc";
import { FileText, Image, Loader2, Palette, Save } from "lucide-react";
import { useEffect, useState } from "react";
import { toast } from "sonner";

function Workspace({ title, eyebrow, children }: { title: string; eyebrow: string; children: React.ReactNode }) {
  return <DashboardLayout><div className="mx-auto max-w-5xl"><header className="mb-7 border-b border-border pb-6"><p className="font-label text-[10px] text-primary">{eyebrow}</p><h1 className="mt-2 font-display text-4xl font-semibold tracking-tight">{title}</h1></header>{children}</div></DashboardLayout>;
}

type LinkItem = { label: string; path: string };
const parseLinks = (source: string, label: string) => { const value = JSON.parse(source); if (!Array.isArray(value) || value.some(item => typeof item?.label !== "string" || typeof item?.path !== "string")) throw new Error(`${label} must be an array of { label, path } objects.`); return value as LinkItem[]; };

export function StudioBranding() {
  const settings = trpc.studio.settings.get.useQuery();
  const update = trpc.studio.settings.update.useMutation({ onSuccess: () => { settings.refetch(); toast.success("Brand settings saved and audited."); }, onError: error => toast.error(error.message) });
  const [name, setName] = useState(""); const [description, setDescription] = useState(""); const [tagline, setTagline] = useState(""); const [logoUrl, setLogoUrl] = useState(""); const [logoAlt, setLogoAlt] = useState(""); const [faviconUrl, setFaviconUrl] = useState(""); const [ogImage, setOgImage] = useState(""); const [primaryColor, setPrimaryColor] = useState("#1b563f"); const [accentColor, setAccentColor] = useState("#e4a741"); const [contactName, setContactName] = useState(""); const [contactEmail, setContactEmail] = useState(""); const [navigation, setNavigation] = useState("[]"); const [footerLinks, setFooterLinks] = useState("[]");
  useEffect(() => { const record = settings.data; if (!record) return; const brand = (record.settings?.brand || {}) as Record<string, string>; const contact = (record.settings?.contact || {}) as Record<string, string>; setName(record.site.name); setDescription(record.site.description || ""); setTagline(brand.tagline || ""); setLogoUrl(brand.logoUrl || ""); setLogoAlt(brand.logoAlt || ""); setFaviconUrl(brand.faviconUrl || ""); setOgImage(brand.defaultOgImageUrl || ""); setPrimaryColor(brand.primaryColor || "#1b563f"); setAccentColor(brand.accentColor || "#e4a741"); setContactName(contact.name || ""); setContactEmail(contact.email || ""); setNavigation(JSON.stringify(record.settings?.navigation || [], null, 2)); setFooterLinks(JSON.stringify(record.settings?.footer_links || [], null, 2)); }, [settings.data]);
  const save = () => { try { const nav = parseLinks(navigation, "Navigation"); const footer = parseLinks(footerLinks, "Footer links"); update.mutate({ name, description: description || undefined, customDomain: settings.data?.site.custom_domain || undefined, themeSettings: (settings.data?.site.theme_settings || {}) as Record<string, unknown>, navigation: nav, defaultLocale: settings.data?.settings?.default_locale || "en", timezone: settings.data?.settings?.timezone || "UTC", seoDefaults: (settings.data?.settings?.seo_defaults || {}) as Record<string, unknown>, featureFlags: (settings.data?.settings?.feature_flags || {}) as Record<string, boolean>, brand: { tagline, logoUrl, logoAlt, faviconUrl, defaultOgImageUrl: ogImage, primaryColor, accentColor }, footerLinks: footer, contact: { name: contactName, email: contactEmail } }); } catch (error) { toast.error(error instanceof Error ? error.message : "Brand settings are invalid."); } };
  return <Workspace title="Brand & site identity" eyebrow="Design · public publication"><div className="grid gap-6 lg:grid-cols-[1.08fr_.92fr]"><section className="rounded-xl border border-border bg-white p-6 shadow-sm"><div className="flex gap-3"><Palette className="h-5 w-5 text-primary" /><div><h2 className="font-display text-2xl font-semibold">Publication identity</h2><p className="mt-1 text-sm text-muted-foreground">These values are used across the public shell, footer, social preview defaults, and Studio.</p></div></div>{settings.isLoading ? <Loader2 className="mx-auto my-16 h-6 w-6 animate-spin text-primary" /> : <div className="mt-6 space-y-4"><div><label className="text-xs font-medium text-muted-foreground">Website name</label><Input value={name} onChange={event => setName(event.target.value)} className="mt-1.5" /></div><div><label className="text-xs font-medium text-muted-foreground">Description</label><textarea value={description} onChange={event => setDescription(event.target.value)} rows={3} className="mt-1.5 w-full rounded-md border border-input bg-background px-3 py-2 text-sm" /></div><div><label className="text-xs font-medium text-muted-foreground">Tagline</label><Input value={tagline} onChange={event => setTagline(event.target.value)} placeholder="A concise statement of your publication's purpose" className="mt-1.5" /></div><div className="grid gap-3 sm:grid-cols-2"><div><label className="text-xs font-medium text-muted-foreground">Primary color</label><Input value={primaryColor} onChange={event => setPrimaryColor(event.target.value)} className="mt-1.5" /></div><div><label className="text-xs font-medium text-muted-foreground">Accent color</label><Input value={accentColor} onChange={event => setAccentColor(event.target.value)} className="mt-1.5" /></div></div><Button onClick={save} disabled={update.isPending} className="gap-2"><Save className="h-4 w-4" />{update.isPending ? "Saving…" : "Save identity"}</Button></div>}</section><section className="rounded-xl border border-border bg-[#fbfcfa] p-6 shadow-sm"><div className="flex gap-3"><Image className="h-5 w-5 text-primary" /><div><h2 className="font-display text-2xl font-semibold">Brand media & links</h2><p className="mt-1 text-sm text-muted-foreground">Use S3 media-library URLs for logos and social images.</p></div></div><div className="mt-6 space-y-4"><div><label className="text-xs font-medium text-muted-foreground">Logo URL</label><Input value={logoUrl} onChange={event => setLogoUrl(event.target.value)} placeholder="https://…" className="mt-1.5" /></div><div><label className="text-xs font-medium text-muted-foreground">Logo alternative text</label><Input value={logoAlt} onChange={event => setLogoAlt(event.target.value)} className="mt-1.5" /></div><div><label className="text-xs font-medium text-muted-foreground">Favicon URL</label><Input value={faviconUrl} onChange={event => setFaviconUrl(event.target.value)} placeholder="https://…" className="mt-1.5" /></div><div><label className="text-xs font-medium text-muted-foreground">Default social-image URL</label><Input value={ogImage} onChange={event => setOgImage(event.target.value)} placeholder="https://…" className="mt-1.5" /></div><div><label className="text-xs font-medium text-muted-foreground">Contact name</label><Input value={contactName} onChange={event => setContactName(event.target.value)} className="mt-1.5" /></div><div><label className="text-xs font-medium text-muted-foreground">Contact email</label><Input value={contactEmail} onChange={event => setContactEmail(event.target.value)} placeholder="editor@example.com" className="mt-1.5" /></div><div><label className="text-xs font-medium text-muted-foreground">Primary navigation JSON</label><textarea value={navigation} onChange={event => setNavigation(event.target.value)} rows={5} className="mt-1.5 w-full rounded-md border border-input bg-white p-3 font-mono text-xs" /></div><div><label className="text-xs font-medium text-muted-foreground">Footer links JSON</label><textarea value={footerLinks} onChange={event => setFooterLinks(event.target.value)} rows={5} className="mt-1.5 w-full rounded-md border border-input bg-white p-3 font-mono text-xs" /></div></div></section></div></Workspace>;
}

export function StudioPages() {
  const [pageType, setPageType] = useState<"privacy" | "terms" | "contact" | "custom">("privacy");
  const page = trpc.studio.pages.get.useQuery({ pageType }); const list = trpc.studio.pages.list.useQuery(); const savePage = trpc.studio.pages.save.useMutation({ onSuccess: () => { page.refetch(); list.refetch(); toast.success("Public page saved and audited."); }, onError: error => toast.error(error.message) });
  const [title, setTitle] = useState("Privacy policy"); const [slug, setSlug] = useState("privacy"); const [html, setHtml] = useState("<h2>Your privacy matters</h2><p>Explain how this publication collects, uses, and protects personal information here. Review this copy for your jurisdiction before publishing.</p>"); const [metaTitle, setMetaTitle] = useState(""); const [metaDescription, setMetaDescription] = useState(""); const [status, setStatus] = useState<"draft" | "published" | "archived">("published");
  useEffect(() => { const data = page.data; if (data) { setTitle(data.title); setSlug(data.slug); setHtml(data.rendered_html); setMetaTitle(data.meta_title || ""); setMetaDescription(data.meta_description || ""); setStatus(data.status); } else { const defaults = { privacy: ["Privacy policy", "privacy"], terms: ["Terms of use", "terms"], contact: ["Contact", "contact"], custom: ["New page", "new-page"] } as const; setTitle(defaults[pageType][0]); setSlug(defaults[pageType][1]); setHtml(pageType === "privacy" ? "<h2>Your privacy matters</h2><p>Explain your data practices here. Review this page for your jurisdiction before publishing.</p>" : "<p>Write this public page here.</p>"); setMetaTitle(""); setMetaDescription(""); setStatus("published"); } }, [page.data, pageType]);
  const save = () => savePage.mutate({ pageType, title, slug, renderedHtml: html, contentJson: { type: "doc", content: [] }, metaTitle: metaTitle || undefined, metaDescription: metaDescription || undefined, status });
  return <Workspace title="Public pages" eyebrow="Legal · contact · custom"><div className="grid gap-6 lg:grid-cols-[.75fr_1.25fr]"><aside className="rounded-xl border border-border bg-white p-5 shadow-sm"><p className="font-label text-[10px] text-primary">Page type</p><Select value={pageType} onValueChange={value => setPageType(value as typeof pageType)}><SelectTrigger className="mt-3"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="privacy">Privacy policy</SelectItem><SelectItem value="terms">Terms of use</SelectItem><SelectItem value="contact">Contact</SelectItem><SelectItem value="custom">Custom page</SelectItem></SelectContent></Select><div className="mt-6 border-t border-border pt-5"><p className="font-label text-[10px] text-muted-foreground">Published pages</p><div className="mt-3 space-y-2">{list.data?.map(item => <div key={item.id} className="rounded-lg bg-muted/60 p-3"><p className="text-sm font-medium">{item.title}</p><p className="mt-1 text-xs text-muted-foreground">/{item.slug} · {item.status}</p></div>)}{!list.data?.length && <p className="text-sm text-muted-foreground">No public pages yet.</p>}</div></div></aside><section className="rounded-xl border border-border bg-white p-6 shadow-sm"><div className="flex gap-3"><FileText className="h-5 w-5 text-primary" /><div><h2 className="font-display text-2xl font-semibold">{title}</h2><p className="mt-1 text-sm text-muted-foreground">Content is sanitized before public rendering. Legal content requires owner or counsel review before release.</p></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><div><label className="text-xs font-medium text-muted-foreground">Page title</label><Input value={title} onChange={event => setTitle(event.target.value)} className="mt-1.5" /></div><div><label className="text-xs font-medium text-muted-foreground">Slug</label><Input value={slug} onChange={event => setSlug(event.target.value.toLowerCase().replace(/[^a-z0-9-]/g, "-"))} className="mt-1.5" /></div></div><div className="mt-4"><label className="text-xs font-medium text-muted-foreground">Sanitized HTML content</label><textarea value={html} onChange={event => setHtml(event.target.value)} rows={12} className="mt-1.5 w-full rounded-md border border-input bg-background p-3 font-mono text-xs leading-5" /></div><div className="mt-4 grid gap-4 sm:grid-cols-2"><div><label className="text-xs font-medium text-muted-foreground">Meta title</label><Input value={metaTitle} onChange={event => setMetaTitle(event.target.value)} className="mt-1.5" /></div><div><label className="text-xs font-medium text-muted-foreground">Meta description</label><Input value={metaDescription} onChange={event => setMetaDescription(event.target.value)} className="mt-1.5" /></div></div><div className="mt-5 flex flex-wrap items-center gap-3"><Select value={status} onValueChange={value => setStatus(value as typeof status)}><SelectTrigger className="w-40"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="published">Published</SelectItem><SelectItem value="draft">Draft</SelectItem><SelectItem value="archived">Archived</SelectItem></SelectContent></Select><Button onClick={save} disabled={savePage.isPending} className="gap-2"><Save className="h-4 w-4" />{savePage.isPending ? "Saving…" : "Save page"}</Button></div></section></div></Workspace>;
}
